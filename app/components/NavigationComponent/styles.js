import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container:{
        flex:1,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 3,
        borderWidth: 0.5,
        borderColor:'red',
        flexBasis: 'auto',
    },
    text:{
        padding: 10,
    }
   
})



